# ============================================================
# Malicious Document Detector — Python Dependencies
# Pinned to match the training environment so the ML model
# (saved with these exact versions) loads correctly in Docker.
# Install:  pip install -r requirements.txt
# ============================================================

# --- Django core ---
Django==6.0.4
djangorestframework==3.17.1
django-cors-headers==4.9.0
djangorestframework-simplejwt==5.5.1

# --- Async task queue ---
celery==5.6.3
redis==7.4.0

# --- File validation (Linux: needs libmagic1 system lib in Dockerfile) ---
python-magic>=0.4.27,<1.0

# --- Document parsing ---
PyPDF2==3.0.1
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
lxml==6.1.0

# --- YARA scanning ---
yara-python==4.5.4

# --- VirusTotal API ---
requests==2.33.1

# --- ML model loading (MUST match the versions the model was trained with) ---
scikit-learn==1.8.0
joblib==1.5.3
xgboost==3.2.0
numpy==2.4.4
pandas==3.0.3
scipy==1.17.1

# --- Email (OTP via Resend) ---
resend==2.30.1

# --- Report export ---
reportlab==4.4.10
pillow==12.2.0

# --- WSGI HTTP Server + static files ---
gunicorn==22.0.0
whitenoise==6.12.0
